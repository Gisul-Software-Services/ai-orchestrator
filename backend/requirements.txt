# Backend API (gateway) — single Python dependency file for this service.
# Used by `backend/gateway/Dockerfile` (build context: `backend/`).
fastapi
uvicorn[standard]
httpx
motor
pymongo
pydantic-settings
redis

# Model-service GPU requirements (heavy ML stack).
# requirements-gpu.txt includes requirements.txt by reference.

-r requirements.txt

torch
transformers

# SentenceTransformer / embeddings (used for FAISS search fallback + metadata embedding)
sentence-transformers

# GPU FAISS replacement
faiss-gpu

# Optional / model-serving helpers
bitsandbytes
accelerate
tf-keras>=2.18.0,<2.19.0

cachetools
motor
pymongo
numpy
pandas
pynvml

